import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Returns Policy | Refunds & Exchanges",
  description:
    "View LKB Jewellers' returns and refund policy. Information on returns, exchanges, and refunds for luxury watches and jewellery purchased from Hatton Garden London.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
